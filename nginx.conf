server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # Identify which frontend served this
        add_header X-Node-ID $hostname always;
        
    }
}